{
  "capability": "environment.validate",
  "description": "代理鉴权拆成两个字段：都给才启用。归一化输出里两个键都在（落库 0600），但视图/日志只给布尔",
  "input": {
    "name": "beta",
    "listen": {
      "port": 16301
    },
    "proxy_user": "alice",
    "proxy_password": "s3cret"
  },
  "expect": {
    "ok": true,
    "proxy_user": "alice",
    "proxy_password": "s3cret"
  }
}
