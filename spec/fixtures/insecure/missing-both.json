{
  "capability": "insecure.hosts",
  "description": "既没有 SNI 也没有地址（或地址为空）= 不放行",
  "input": {
    "hosts": [
      "app.example.com"
    ]
  },
  "expect": {
    "ok": true,
    "match": false
  }
}
