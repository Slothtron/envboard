//! 工程门禁的宿主 crate —— 真正的断言在 `tests/<gate>.rs`。
//!
//! 这里没有库代码，只有那条要求：**仓库的工程纪律必须由仓库自己的语言来判定**。
//! 每个门禁是一个独立的 test target（一文件一门禁），可以单跑、可以 `--nocapture`
//! 看完整问题清单；`ci/verify.sh` 只负责按层与顺序编排它们。

#![doc = ""]
