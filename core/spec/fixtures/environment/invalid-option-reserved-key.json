{
  "capability": "environment.validate",
  "description": "反例：选项键命中 denylist（管理器自有键 listen_port），必须响亮失败而不是被透传",
  "input": {
    "name": "bad",
    "listen": { "port": 16306 },
    "options": { "listen_port": "9999" }
  },
  "expect": {
    "ok": false,
    "code": "invalid_config",
    "field": "environment.options.listen_port"
  }
}
